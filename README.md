# dotfiles

Personal configuration files and shared development-tool settings. The
repository is also used as the source for a Docker-based Linux development
environment with Fish, mise, Neovim, and AI CLI tools.

## Homebrew

On the macOS host machine, install Homebrew and run the package setup script:

```sh
./brew.sh
```
